import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    globalContainer: {
        flex:1,
    },
    scrollViewContainer: {
        padding: 16 
    },
    screenshotImage: {
        width: "100%", 
        height: 200, 
        marginBottom: 16 
    }

})